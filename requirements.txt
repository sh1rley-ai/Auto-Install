# Core dependencies (only what the code imports today; later phases append their own)
langgraph>=0.2
openai>=1.0.0
dashscope>=1.14.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
